import express from 'express';

// Funzioni per gestire le prenotazioni
import * as bookinController from '../controllers/bookinController.js';

// Creazione del router
const router = express.Router();

// Booking routes


export default router;